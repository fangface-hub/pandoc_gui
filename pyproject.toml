[project]
name = "pandoc-gui"
version = "1.4.0"
description = "A GUI application for document conversion using Pandoc"
requires-python = ">=3.14"

[dependency-groups]
build = [
    "nuitka>=4.1.3",
]