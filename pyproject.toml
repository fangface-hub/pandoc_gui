[project]
name = "pandoc-gui"
version = "1.3.4"
description = "A GUI application for document conversion using Pandoc"
requires-python = ">=3.14"

[dependency-groups]
build = [
    "pyinstaller>=6.0",
]